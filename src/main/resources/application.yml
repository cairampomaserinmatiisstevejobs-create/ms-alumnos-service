server:
  port: 8086

spring:
  webflux:
    base-path: /api/v1
  r2dbc:
    url: r2dbc:h2:mem:///alumnos;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

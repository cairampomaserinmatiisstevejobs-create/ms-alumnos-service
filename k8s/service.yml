apiVersion: v1
kind: Service
metadata:
  name: ms-alumnos-service
  namespace: msirildev
spec:
  selector:
    app: ms-alumnos-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8086
  type: ClusterIP
